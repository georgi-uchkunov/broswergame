package st.pro.browsergame.security;

import org.springframework.stereotype.Service;

@Service
public class ApplicationDetailsService {
	/*
	 * implements UserDetailsService {
	 * 
	 * private UserRepository userRepo;
	 * 
	 * @Autowired public ApplicationDetailsService(UserRepository userRepo) {
	 * this.userRepo = userRepo; }
	 * 
	 * @Override public UserDetails loadUserByUsername(String email) throws
	 * UsernameNotFoundException {
	 * 
	 * final User user = userRepo.findByEmail(email); if (null == user) { throw new
	 * UsernameNotFoundException("User with email=''" + email +
	 * "'' does not exist!"); } return new UserPrinciple(user); }
	 * 
	 */}
